package ec.y{{YEAR}}

import ec.Quest

/** Quests for {{YEAR}}. `make new` appends here; the compiler checks every entry exists. */
val quests: Map<Int, () -> Quest> = mapOf(
    // make new: quests
)
